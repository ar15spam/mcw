# ---------------------------------------------------------------------------
# MIDICOLLAB web (Next.js) environment
# Copy to .env.local for local dev; set the same keys in Vercel for production.
# ---------------------------------------------------------------------------

# Rust realtime backend (Railway). Use https:// and wss:// in production.
NEXT_PUBLIC_API_URL=https://your-studio-backend.up.railway.app
NEXT_PUBLIC_WS_URL=wss://your-studio-backend.up.railway.app/ws

# Neon Postgres (pooled connection string).
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Better Auth. Generate the secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-with-a-strong-random-secret
# Local: http://localhost:3000  ·  Production: your deployed https origin
BETTER_AUTH_URL=http://localhost:3000

# Shared HMAC secret between this app and the Rust realtime backend.
# MUST be byte-for-byte identical here and in the Railway service.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
REALTIME_SHARED_SECRET=replace-with-a-strong-random-secret

# ---- AI co-producer ---------------------------------------------------------
# Set ONE of these for real open-ended language understanding. With neither,
# the co-producer still runs in "offline mode" (a pattern matcher that handles
# common requests) — set AGENT_OFFLINE=0 to turn it off entirely.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# AGENT_PROVIDER=anthropic        # force a provider when both keys are set
# AGENT_MODEL=claude-sonnet-5     # override the per-provider default model
# AGENT_REASONING_EFFORT=low      # OpenAI reasoning models (gpt-5.x/o-series): low|medium|high|off
# AGENT_OFFLINE=0                 # disable the no-key fallback
#
# OpenAI note: the adapter uses the /v1/responses API, so reasoning models
# (gpt-5.x, o-series) work directly. A custom OPENAI_BASE_URL must support it.

# ---- Optional: Google OAuth --------------------------------------------------
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=true
